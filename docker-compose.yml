x-arrowflight-image: &arrowflight-image
  image: arrowflight-test:latest
  build:
    context: .
    dockerfile: docker/Dockerfile

x-server-env: &server-env
  FLIGHT_DATA_DIR: /data/parquet
  FLIGHT_PORT: "32010"
  FLIGHT_HOSTS: "${FLIGHT_HOSTS:-flight-server-1,flight-server-2,flight-server-3}"
  HAZELCAST_PORT: "5701"
  JAVA_OPTS: -Xmx2g

services:
  spark-master:
    <<: *arrowflight-image
    command: ["spark-master"]
    ports:
      - "7077:7077"
      - "8080:8080"

  spark-worker-1:
    <<: *arrowflight-image
    command: ["spark-worker"]
    environment:
      SPARK_WORKER_CORES: "2"
      SPARK_WORKER_MEMORY: 2g
      SPARK_WORKER_WEBUI_PORT: "8081"
    depends_on:
      - spark-master
    volumes:
      - generated-data:/generated
    ports:
      - "8081:8081"

  spark-worker-2:
    <<: *arrowflight-image
    command: ["spark-worker"]
    environment:
      SPARK_WORKER_CORES: "2"
      SPARK_WORKER_MEMORY: 2g
      SPARK_WORKER_WEBUI_PORT: "8082"
    depends_on:
      - spark-master
    volumes:
      - generated-data:/generated
    ports:
      - "8082:8082"

  spark-benchmark-publisher:
    <<: *arrowflight-image
    profiles: ["benchbase"]
    command: ["publish-benchmark-data"]
    environment:
      SPARK_MASTER_URL: spark://spark-master:7077
      SPARK_DRIVER_HOST: spark-benchmark-publisher
      BENCHMARK_DATASET: "${BENCHMARK_DATASET:-tpch}"
      BENCHMARK_LOAD_SCHEMA: "${BENCHMARK_LOAD_SCHEMA:-default}"
      BENCHMARK_SCHEMA: "${BENCHMARK_SCHEMA:-tpch}"
      PUBLISH_ACTION: "${PUBLISH_ACTION:-all}"
      FLIGHT_SOURCE_HOST: "${FLIGHT_SOURCE_HOST:-flight-server-1}"
      FLIGHT_SOURCE_PORT: "${FLIGHT_SOURCE_PORT:-32010}"
      FLIGHT_SERVER_DATA_DIRS: "${FLIGHT_SERVER_DATA_DIRS:-/server-data/flight-server-1,/server-data/flight-server-2,/server-data/flight-server-3}"
      TEST_PARTITIONS: "${TEST_PARTITIONS:-24}"
      SPARK_WAREHOUSE_DIR: /spark-warehouse
      SPARK_METASTORE_DB: /spark-warehouse/metastore_db
    volumes:
      - generated-data:/generated
      - flight-server-1-data:/server-data/flight-server-1
      - flight-server-2-data:/server-data/flight-server-2
      - flight-server-3-data:/server-data/flight-server-3
      - spark-warehouse:/spark-warehouse
    restart: "no"

  spark-benchmark-creator:
    <<: *arrowflight-image
    profiles: ["benchbase"]
    command: ["create-benchmark-tables"]
    environment:
      SPARK_MASTER_URL: spark://spark-master:7077
      SPARK_DRIVER_HOST: spark-benchmark-creator
      BENCHMARK_DATASET: "${BENCHMARK_DATASET:-tpch}"
      BENCHMARK_LOAD_SCHEMA: "${BENCHMARK_LOAD_SCHEMA:-default}"
      SPARK_WAREHOUSE_DIR: /spark-warehouse
      SPARK_METASTORE_DB: /spark-warehouse/metastore_db
    volumes:
      - spark-warehouse:/spark-warehouse
    restart: "no"

  spark-thrift-server:
    <<: *arrowflight-image
    profiles: ["benchbase"]
    command: ["spark-thrift-server"]
    environment:
      SPARK_MASTER_URL: spark://spark-master:7077
      SPARK_DRIVER_HOST: spark-thrift-server
      SPARK_THRIFT_BIND_HOST: 0.0.0.0
      SPARK_THRIFT_PORT: "10000"
      SPARK_WAREHOUSE_DIR: /spark-warehouse
      SPARK_METASTORE_DB: /spark-warehouse/metastore_db
    ports:
      - "10000:10000"
      - "4040:4040"
    volumes:
      - spark-warehouse:/spark-warehouse
    healthcheck:
      test: ["CMD-SHELL", "bash -c '</dev/tcp/127.0.0.1/10000'"]
      interval: 5s
      timeout: 3s
      retries: 60

  benchbase-spark:
    build:
      context: ./benchmarks/benchbase-spark
      args:
        BENCHBASE_IMAGE: "${BENCHBASE_IMAGE:-benchbase.azurecr.io/benchbase:latest}"
    profiles: ["benchbase"]
    environment:
      BENCHBASE_PROFILE: postgres
    depends_on:
      spark-thrift-server:
        condition: service_healthy
    volumes:
      - ./benchmarks/benchbase-spark/config:/benchbase/config/custom:ro
      - ./benchmarks/benchbase-spark/results:/benchbase/results

  flight-server-1:
    <<: *arrowflight-image
    command: ["server"]
    environment:
      <<: *server-env
      FLIGHT_LOCALHOST: flight-server-1
    volumes:
      - flight-server-1-data:/data/parquet
    ports:
      - "32010:32010"
    healthcheck:
      test: ["CMD-SHELL", "bash -c '</dev/tcp/$${FLIGHT_LOCALHOST}/$${FLIGHT_PORT}'"]
      interval: 5s
      timeout: 3s
      retries: 60

  flight-server-2:
    <<: *arrowflight-image
    command: ["server"]
    environment:
      <<: *server-env
      FLIGHT_LOCALHOST: flight-server-2
    volumes:
      - flight-server-2-data:/data/parquet
    ports:
      - "32011:32010"
    healthcheck:
      test: ["CMD-SHELL", "bash -c '</dev/tcp/$${FLIGHT_LOCALHOST}/$${FLIGHT_PORT}'"]
      interval: 5s
      timeout: 3s
      retries: 60

  flight-server-3:
    <<: *arrowflight-image
    command: ["server"]
    environment:
      <<: *server-env
      FLIGHT_LOCALHOST: flight-server-3
    volumes:
      - flight-server-3-data:/data/parquet
    ports:
      - "32012:32010"
    healthcheck:
      test: ["CMD-SHELL", "bash -c '</dev/tcp/$${FLIGHT_LOCALHOST}/$${FLIGHT_PORT}'"]
      interval: 5s
      timeout: 3s
      retries: 60

volumes:
  generated-data:
  flight-server-1-data:
  flight-server-2-data:
  flight-server-3-data:
  spark-warehouse:
